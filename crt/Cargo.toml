[package]
name = "purert"
version = "0.0.1"
edition = "2021"
description = "Freestanding runtime for purestd: the process entry point and the toolchain-expected symbols (mem*, unwind stubs, getauxval) that, in a hosted build, come from crt0/compiler_builtins/the unwinder rather than from std."
license = "MIT OR Apache-2.0"

[lib]
